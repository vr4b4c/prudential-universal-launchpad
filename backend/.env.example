# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=universal_launch_pad

# JWT (set JWT_SECRET in production)
JWT_SECRET=your-secret-change-in-production
JWT_EXPIRES_IN=7d

# Environment
NODE_ENV=development

# Initial users: run `pnpm run seed` after migrations to create default users (see scripts/seed.ts)
